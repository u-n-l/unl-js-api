/**
 * The type representing the configuration necessary to initialize the SDK.
 * @alias UnlApiConfig
 */
export type UnlApiConfig = {
  /**
   * Api key used to make the requests.
   */
  apiKey: string;
};
